import type { NextConfig } from "next";


const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  
};

// module.exports = {
//   allowedDevOrigins: ['marina-conjuror-unlearned.ngrok-free.dev'],
// }

export default nextConfig;
